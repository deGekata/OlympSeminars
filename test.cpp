#include "Structures/BinHeap.cpp"
#include <stdio.h>

int main() {
    heap_type arr[10] = {2, 3, 4, 1, 5, 8, 7, 6, 9, 10};
    binHeap* heap = createHeap(arr, 10);
    
    // printf("%%");
    return 0;
}